#![allow(dead_code)]
#![allow(unused_variables)]
extern crate regex;

mod matching;
mod error_handling;
mod ownership;

fn main() {
    //matching::matching();
    //error_handling::errors();
    ownership::ownership();
}
